{% include 'header' %}

#import "{{ prefix }}{{ module_info.name }}Assembly.h"

#import "{{ prefix }}{{ module_info.name }}Protocols.h"

#import "{{ prefix }}{{ module_info.name }}ViewController.h"
#import "{{ prefix }}{{ module_info.name }}ViewModel.h"

#import "{{ prefix }}{{ module_info.name }}InputData.h"

@implementation {{ prefix }}{{ module_info.name }}Assembly

+ (UIViewController *)assemblyWithInputData:({{ prefix }}{{ module_info.name }}InputData *)inputData {
    
    /* view */
    {{ prefix }}{{ module_info.name }}ViewController *viewController = [{{ prefix }}{{ module_info.name }}ViewController new];
    
    /* model */
    {{ prefix }}{{ module_info.name }}ViewModel *viewModel = [{{ prefix }}{{ module_info.name }}ViewModel new];
    viewModel.inputData = inputData;
    
    /* router */
    // {{ prefix }}{{ module_info.name }}Router *router = [{{ prefix }}{{ module_info.name }}Router new];
    
    /* assembly */
    viewController.viewModel = viewModel;
    // viewController.router = router;
    viewModel.view = viewController;
    // viewModel.router = router;
    // router.view = viewController;
    // router.viewModel = viewModel;
    
    return viewController;
}

@end

